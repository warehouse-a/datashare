spring:
  profiles:
    active:
    - dev
---
server:
  port: 9527
spring:
  application:
    name: datazuul

feign:
  hystrix:
    enabled: true
ribbon:
  ReadTimeout: 60000
  ConnectTimeout: 60000


zuul:
  ignored-services: "*"
  routes:
    mydept.serviceId: datauser
    mydept.path: /user/**
info:
  name: zuul