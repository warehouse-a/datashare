spring:
  profiles:
    active:
    - dev
---
server:
  port: 8002

feign:
  hystrix:
    enabled: true

spring:
  profiles: dev
  application:
    name: datamenu
  datasource:
    username: temp
    password: temp
    url: jdbc:mysql://39.106.207.72:3306/datamenu
    dbcp2:
      max-idle: 5
      min-idle: 5
      max-wait-millis: 200
      initial-size: 5
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource


mybatis:
  config-location: classpath:mybatis/mybatis.cfx.xml
  type-aliases-package: cn.com.datashare.datamenu.bean
  mapper-locations:
  - classpath:mybatis/mapper/**/*.xml

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
  instance:
    instance-id: datamenu
    prefer-ip-address: true